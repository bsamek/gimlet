package github

import (
	"testing"
)

func TestGithubAuthManager(t *testing.T) {
	// assert := assert.New(t)
	// g := evergreen.GithubAuthConfig{
	// 	ClientId:     "foo",
	// 	ClientSecret: "bar",
	// 	Organization: "",
	// }
	// authConfig := evergreen.AuthConfig{
	// 	Github: &g,
	// }
	// userManager, isLDAP, err := LoadUserManager(authConfig)
	// assert.Nil(err)
	// assert.False(isLDAP)
	// assert.NotNil(userManager.GetLoginHandler(""))
	// assert.NotNil(userManager.GetLoginCallbackHandler())
}
